@click - downloads a python package for implementing CLI rfaces. it provides decorators functions to easily build applications.

The @click.group() decorator transforms the main() function into a Click command group. A group can contain multiple subcommands. This creates the base command pycodeadvisor that can have subcommands like pycodeadvisor check.